doctype html
html(lang="de")
    head
        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no")
        title Test
        link(rel="stylesheet" href="#asset(fa5.css)")
        link(rel="stylesheet" href="#asset(style.css)")

        script(type="text/javascript" src="#asset(vue.min.js)")
        script(type="text/javascript" src="#asset(dwarf.js)")
        script(type="text/javascript" src="#asset(deraner.js)")
        script(type="text/javascript" src="#asset(app.js)")
    body
        h1
            i.fas.fa-user
            div #{template.name}: #{template.env.test}
            div #{env.APP_ENV}